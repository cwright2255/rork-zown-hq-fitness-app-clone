{
  "name": "zown-functions",
  "scripts": {
    "build": "echo 'No build step needed - plain JavaScript, nothing to compile'",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "openai": "^4.0.0"
  },
  "main": "src/index.js",
  "engines": { "node": "20" }
}
